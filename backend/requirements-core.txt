# Minimal dependencies to run the API and the command-line scripts. Left
# unpinned so pip installs versions that support current Python releases
# (including 3.14). The code uses stable FastAPI / Pydantic / SQLAlchemy APIs,
# so the latest versions work fine.
#
# For an actual deployment use requirements-server.txt instead: it pins exact
# versions and adds the websocket, Redis and narrative-layer dependencies.
fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
python-multipart
httpx
